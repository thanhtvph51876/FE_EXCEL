# File Retention Policy

Uploaded source files and generated output files are stored in local storage during the current MVP.

Users may delete uploaded files from the workspace. Generated outputs are stored as output records for download history and operational audit.

Production deployments should configure a retention period, regular storage backups, and restore testing before selling to external customers at scale.
